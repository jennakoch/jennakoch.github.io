import { FilterNameTypePipe } from './filter-name-type.pipe';

describe('FilterNameTypePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterNameTypePipe();
    expect(pipe).toBeTruthy();
  });
});
